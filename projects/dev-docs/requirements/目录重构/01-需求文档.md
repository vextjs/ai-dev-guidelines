# 目录结构重构

> **项目**: ai-dev-guidelines (dev-docs)  
> **日期**: 2026-02-28  
> **优先级**: P1
> 
> **关联**: 本文档 → [02-技术方案](./02-技术方案.md) → [03-实施方案/](./03-实施方案/README.md)

---

## 一、需求背景

ai-dev-guidelines 当前有 9 个平级子目录（workflows/、standards/、templates/、tools/、spec-self-fix/、best-practices/、examples/、docs/、changelogs/），外加 projects/。目录层级过于扁平，缺乏语义分组，AI 和用户在浏览时难以快速理解各目录的职能归属关系。

需要将目录收拢为以受众和职能为核心的分层结构：
- `core/` — AI 执行内核（规则、流程、工具、模板、自修复）
- `docs/` — 文档区（用户指南、最佳实践、示例）
- `projects/` — 项目产出（不变）
- `changelogs/` — 版本变更（不变）

---

## 二、需求内容

### 功能需求

- [ ] F1: 创建 `core/` 目录，将 `workflows/`、`standards/`、`templates/`、`tools/` 移入
- [ ] F2: 将 `spec-self-fix/` 移入 `core/` 并重命名为 `self-fix/`
- [ ] F3: 将 `best-practices/` 和 `examples/` 移入 `core/`（AI 参考材料）
- [ ] F4: 将规范顶级文件（README.md、QUICK-REFERENCE.md、CONSTRAINTS.md、ONBOARDING.md、STATUS.md、META.yaml）移入 `core/`
- [ ] F5: 将原 `docs/` 下所有内容移入 `docs/guides/`
- [ ] F6: 新建根目录 `README.md`（项目总说明，简洁入口）
- [ ] F7: 新建 `docs/README.md`（文档区索引）
- [ ] F8: `CHANGELOG.md` 留在根目录（与 changelogs/ 配套）
- [ ] F9: `projects/` 和 `changelogs/` 位置不变
- [ ] F10: 更新 `.github/copilot-instructions.md` 路径指向 `core/`
- [ ] F11: 所有内部路径引用同步更新
- [ ] F12: 工具脚本（JS 文件）中的路径常量更新
- [ ] F13: `validate-structure.js` 的 EXPECTED_STRUCTURE 更新

### 非功能需求

- 兼容性要求: 历史文件（changelogs/、reports/、.ai-memory/）中的旧路径不修改，保持历史快照完整性
- 可回滚: 所有变更通过 git 管理，可 `git reset --hard` 回退

---

## 三、验收标准

| 验收项 | 标准 |
|-------|------|
| 根目录 | 只剩 README.md + CHANGELOG.md + core/ + docs/ + projects/ + changelogs/ + .github/ |
| core/ 结构 | 包含所有规范文件（README.md、QUICK-REFERENCE.md 等）+ 所有子目录（workflows/ 等） |
| 内部链接 | 所有 P0/P1 文件中的路径引用正确 |
| copilot-instructions | 所有路径指向 core/ 下新位置 |
| 工具脚本 | `node core/tools/validate-structure.js` 正常运行 |
| `bump-version.js` | `node core/tools/bump-version.js` 正常运行 |
| 历史文件 | changelogs/、reports/、.ai-memory/ 中的旧路径保持原样 |

---

## 四、备注

- 本需求源自用户对目录结构的优化建议
- 上次会话（不同编辑器）已做过初步分析，核心发现已纳入本需求
- 路径映射规则明确，可脚本化批量替换


