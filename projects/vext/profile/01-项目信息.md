# vext - 项目信息

> **项目名称**: vext  
> **框架**: Hono (当前) → 自研可替换底层  
> **运行时**: Node.js  
> **语言**: TypeScript  
> **最后更新**: 2026-02-27

---

## 📍 项目路径

```yaml
源码目录: vext/                    # 工作区根目录下
规范目录: ai-dev-guidelines/projects/vext/
架构方案: ai-dev-guidelines/projects/vext/plans/
```

---

## 🛠️ 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 运行时 | Node.js | — |
| 语言 | TypeScript | 类型安全 |
| 底层框架 | Hono（当前） | 通过 Adapter 架构支持替换底层框架 |
| 校验 | schema-dsl | 参数校验模块 |
| 架构模式 | Adapter + 插件系统 | v2 方案：底层框架可替换 + 插件扩展 |
| 请求上下文 | AsyncLocalStorage | 请求级信息贯穿 route → service → model |

---

## 📂 目录结构

```
vext/
├── lib/                    # 框架核心库
├── routes/                 # 路由定义（三段式：path, options, handler）
├── services/               # 服务层（类的方式，注入到 app.services）
├── middlewares/             # 中间件
├── plugins/                # 插件（在 app 对象上注册方法/属性/路由）
├── utils/                  # 工具函数
├── config/                 # 配置文件（通过 app.config 访问）
├── models/                 # 数据模型（待开发）
├── public/                 # 静态资源（待开发）
└── views/                  # 视图模板（待开发）
```

---

## 🏗️ 核心架构概念

### 路由三段式

```typescript
// path: 路由路径
// options: 声明式配置对象（中间件、参数校验、文档元信息等）
// handler: 路由处理函数
route('POST /users', {
  middleware: [auth],
  validate: { body: userSchema },
  docs: { summary: '创建用户' }
}, async (req, res) => {
  const user = await app.services.user.create(req.body);
  res.json(user);
});
```

### 核心对象

| 对象 | 用途 | 访问方式 |
|------|------|---------|
| `app` | 应用实例，挂载全局资源 | `app.db`、`app.config`、`app.services.xxx` |
| `req` | 请求上下文（封装底层框架的请求） | handler 参数 |
| `res` | 响应对象 | `res.json()`，抛出异常自动捕获并返回错误响应 |

### 服务层

```typescript
// 所有 service 文件注入到 app 对象
// 通过 app.services.<name> 方式访问
class UserService {
  async create(data: CreateUserDTO) {
    return app.db.users.insert(data);
  }
}
// 访问: app.services.user.create(...)
```

### 插件机制

```typescript
// 插件可以在 app 对象上注册方法、属性、路由
const myPlugin = {
  name: 'my-plugin',
  register(app) {
    app.decorate('myUtil', () => { /* ... */ });
    app.addRoute('GET /plugin-route', {}, handler);
  }
};
```

---

## 📦 依赖关系

| 依赖 | 类型 | 说明 |
|------|------|------|
| hono | 运行时 | 当前底层 HTTP 框架（后期可替换） |
| schema-dsl | 运行时 | 参数校验模块 |

---

## 🔄 项目状态

| 项目 | 状态 |
|------|------|
| 当前阶段 | 架构设计中（v2 方案已输出，待确认） |
| 架构方案 | v1（初版）→ v2（Adapter 架构 + 插件系统） |
| 待开发模块 | models、public、views |
| 关键待解决 | 请求上下文传递、service 类型安全、OpenAPI 文档自动生成 |

---

## 📚 相关文档

- **架构方案**: [plans/README.md](../plans/README.md)
- **v2 方案详情**: [plans/v2/README.md](../plans/v2/README.md)
- **原始需求**: [需求.txt](../需求.txt)
- **通用开发规范**: `../../core/standards/` 目录下的各规范文件