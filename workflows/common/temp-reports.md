# 临时报告规范

> AI 任务过程中产生的临时分析报告的存储和管理规范

---

## 🎯 解决的问题

- 各项目 `reports/` 目录下文件平铺无结构（如 payment 有 20+ 个平铺文件）
- 报告命名无规范，难以检索
- 没有定义哪些该保留、哪些该清理
- **报告缺少日期，无法判断哪个是最新版本**

---

## 📁 存储位置

```
ai-dev-guidelines/projects/<project>/          # 如 projects/chat/
└── reports/                          # 🔴 AI 临时报告（gitignore 忽略）
    ├── diagnostics/                  # 诊断分析报告
    ├── bugs/                         # Bug 分析报告
    ├── requirements/                 # 需求分析报告
    ├── optimizations/                # 优化分析报告
    ├── analysis/                     # 深度分析/架构分析报告
    └── .temp/                        # 临时/中间过程文件（可随时清理）
```

> **注意**: `reports/` 在 ai-dev-guidelines/.gitignore 中忽略，不提交到 git。

---

## 📝 命名规范

### 🔴 强制规则：文件名必须以日期开头

```yaml
格式: <YYYYMMDD>-<NN>-<类型>-<简述>.md

  YYYYMMDD : 8 位日期（强制，无分隔符）
  NN       : 2 位当日序号（从 01 开始递增）
  类型     : 报告类型前缀（见下方）
  简述     : 具体描述（小写+连字符）

🔴 日期前缀为强制要求（NO EXCEPTIONS）:
  - 必须使用真实日期，格式 YYYYMMDD（8位数字，无分隔符）
  - 日期必须是报告创建的实际日期
  - 禁止省略日期前缀
  - 禁止使用其他日期格式（如 2026-02-26、26Feb 等）

🔴 当日序号为强制要求:
  - 日期之后必须跟 2 位序号（01、02、03...）
  - AI 生成报告前必须扫描目标目录，统计当天已有的报告数量，取 max+1
  - 如果当天没有报告，序号为 01
  - 序号确保同一天的报告有明确的先后顺序

  获取序号的方法:
    1. 扫描目标 reports/ 目录下以 YYYYMMDD- 开头的文件
    2. 提取已有序号的最大值
    3. 新文件序号 = 最大值 + 1（不存在则为 01）

类型前缀（序号之后）:
  diag-    : 诊断报告
  bug-     : Bug 分析
  req-     : 需求分析
  opt-     : 优化分析
  review-  : 代码审查
  analysis-: 深度分析/架构分析
  research-: 技术调研
  verify-  : 验证报告（对已有报告的交叉验证）

✅ 正确示例:
  20260226-01-bug-subscription-renewal-duplicate.md    # 当天第 1 个报告
  20260226-02-analysis-v3-architecture.md              # 当天第 2 个
  20260226-03-verify-architecture.md                   # 当天第 3 个
  20260228-01-analysis-hot-reload.md                   # 另一天第 1 个

❌ 错误示例:
  v3-architecture-deep-analysis.md          # 缺少日期和序号
  20260226-analysis-xxx.md                  # 缺少序号
  20260226-1-analysis-xxx.md                # 序号必须 2 位（01 不是 1）
  2026-02-26-01-bug-something.md            # 日期格式错误（有分隔符）
  analysis.md                               # 缺少全部前缀
```

### 为什么日期前缀是强制的？

1. **版本识别**: 同一主题可能有多次分析，日期是判断新旧的唯一依据
2. **快速排序**: 文件管理器中按名称排序即可按时间排序
3. **清理依据**: 基于日期可以快速识别过期报告
4. **跨编辑器/跨 Agent**: 多个 AI 助手可能生成同主题报告，日期+Agent 可区分

---

## 📋 报告内容必填字段

### 🔴 报告头部必须包含

每份报告的头部**必须**包含以下元信息，**禁止留占位符**：

```markdown
# [报告标题]

> **项目**: [真实项目名]
> **类型**: [诊断/Bug分析/需求分析/优化/深度分析/...]
> **创建日期**: YYYY-MM-DD ← 🔴 必须替换为真实日期
> **Agent**: [zed-copilot / webstorm-copilot / cursor / ...] ← 🔴 必须标识
> **状态**: 📝 进行中 / ✅ 已完成

---
```

```yaml
❌ 绝对禁止:
  - 日期字段保留 "YYYY-MM-DD" 占位符
  - 日期字段留空
  - 不填写 Agent 标识
  - 文件名不带日期前缀

✅ AI 自检清单（生成报告前必须确认）:
  - [ ] 文件名以 YYYYMMDD 开头？
  - [ ] 头部「创建日期」已替换为真实日期？
  - [ ] 头部「Agent」已标识当前编辑器/AI？
  - [ ] 报告存放在正确的子目录下？
```

---

## 🔄 与 ai-dev-guidelines 的关系

```yaml
reports/ (ai-dev-guidelines/projects/<project>/ 下):
  - 临时工作文件，不提交 git
  - AI 分析过程中随时写入
  - 可做参考但不是正式文档

ai-dev-guidelines/projects/<project>/requirements/ 等 (归档文档):
  - 正式归档文档，提交 git
  - 完整模式下生成
  - 长期保存，可追溯
```

---

## ⚙️ AI 执行规范

```yaml
写入时机:
  - 分析代码时生成诊断报告 → reports/diagnostics/
  - Bug 定位时生成分析报告 → reports/bugs/
  - 需求分析时生成临时方案 → reports/requirements/
  - 深度分析/架构分析报告 → reports/analysis/
  - 中间过程文件 → reports/.temp/

🔴 写入前必须:
  1. 获取当前真实日期
  2. 扫描目标目录，统计当天已有报告数量，确定序号（max+1 或 01）
  3. 按命名规范组装文件名（YYYYMMDD-NN-类型-简述.md）
  4. 使用标准头部模板并填充所有必填字段
  5. 自检: 文件名包含日期+序号、内容包含真实日期和 Agent

清理策略:
  - .temp/ 下文件可随时清理
  - 其他目录保留 30 天
  - 用户可以说"清理临时报告"触发
```

---

## 📊 同主题多版本报告

当同一天产生多次报告时，序号自然区分先后顺序：

```yaml
示例 — 一天内做了 5 个报告:
  20260228-01-analysis-architecture.md          # 上午：架构总体分析
  20260228-02-analysis-hot-reload.md            # 上午：热重载专项分析
  20260228-03-verify-architecture.md            # 下午：对 01 的验证
  20260228-04-analysis-cluster.md               # 下午：集群分析
  20260228-05-verify-architecture-2nd.md        # 晚上：第二次验证

识别最新版本:
  - 同一天：序号最大的即为最新
  - 跨天：日期最大的即为最新
  - 文件管理器中按文件名排序 = 按时间排序

建议:
  - 最新版本的报告可以在头部注明"替代: 20260228-01-analysis-architecture.md"
  - 旧版本不必删除，30 天后自动清理
```

---

**版本**: v1.3
**创建日期**: 2026-02-24
**最后更新**: 2026-02-27
**v1.3 变更**: 报告命名改为 `YYYYMMDD-NN-<类型>-<简述>.md`（NN 为当日序号递增），去掉文件名中的 Agent 前缀（由目录隔离保证）
**v1.2 变更**: 报告命名格式加入 Agent 标识（已由 v1.3 替代）
**v1.1 变更**: 新增日期前缀强制规则、报告头部必填字段规范
