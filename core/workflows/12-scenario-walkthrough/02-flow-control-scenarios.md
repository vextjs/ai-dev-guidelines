# 流程控制场景走查用例

> 覆盖预检查、记忆写入、继续任务、意图推断、确认点、统一流程验证、出口门禁等流程控制机制，验证 AI 在非工作流专属逻辑上的行为合规性

**用例数量**: 13  
**分类**: flow-control  
**前置条件**: AI 已加载 `core/QUICK-REFERENCE.md`，规范体系正常

---

## 📋 用例总览

| ID | 场景 | 用例名称 | 核心验证点 |
|----|------|---------|-----------|
| F01 | 预检查 | 预检查 5 项完整输出 | 5 项 + 第 6 行硬性阻塞 |
| F02 | 阶段 0 | 阶段 0 记忆写入硬性阻塞 | tool call 顺序约束 + 记忆文件创建 |
| F03 | 继续任务 | 跨会话任务恢复 | 读记忆 → 读报告 → 上下文恢复 |
| F04 | 意图推断 | 意图推断判定（分析+变更混合场景） | Layer 3 三问 → 判定任务类型（FIX-010 升级为 Layer 3） |
| F05 | CP1 确认点 | CP1 需求理解后暂停 | 无论需求多清晰都必须暂停 |
| F06 | CP2 确认点 | CP2 技术方案后暂停 | 方案和执行不在同一回复 |
| F07 | CP3 确认点 | CP3 实施方案后暂停 | 实施方案含 IMPLEMENTATION-PLAN |
| F08 | 多任务拆分 | 多任务同时提出 | 识别并建议分开执行（约束 #4） |
| F09 | 小需求流程 | 小需求统一 7 阶段流程 | 小需求仍走 7 阶段 + 归档文档文件必须存在（FIX-016） |
| F10 | 大需求流程 | 大需求统一 7 阶段流程 | 大需求 core 模板 + 完整文档套件（FIX-016） |
| F11 | ~~模式切换~~ | [DEPRECATED] 模式切换（v2.13.0 废弃） | v2.13.0 起不再存在模式切换场景 |
| F12 | 出口门禁 | 阶段 4.5 执行合规性回溯检查 | 入口-出口闭环验证 |
| F13 | 报告+记忆自动输出 | 自动输出不询问 | 报告+记忆自动写入（约束 #17） |

---

## 用例详情

```yaml
# ═══════════════════════════════════════════════════════════
# F01 — 预检查 5 项完整输出
# ═══════════════════════════════════════════════════════════

- id: F01
  name: "预检查 5 项完整输出"
  category: flow-control
  source_ref: "core/workflows/00-pre-check/README.md, core/QUICK-REFERENCE.md §预检查"

  simulated_input: "在 chat 项目实现消息已读功能"

  expected_behavior:
    - "AI 收到消息后，首先输出预检查 6 行（1.工作区 2.任务类型 3.输出位置 4.Agent 5.上次记忆 6.📝记忆已创建）"
    - "第 6 行输出前，禁止执行任何分析性文件读取"

  checkpoints:
    - "6 行全部输出，无遗漏"
    - "第 5 行使用 list_directory 逐层扫描（非 glob）"
    - "第 6 行在分析开始前已输出"
    - "Agent 标识与当前编辑器+AI 组合一致"
    - "输出位置路径包含 projects/<project>/ 前缀"

  pass_criteria: "预检查 6 行完整输出，第 6 行在任何分析之前完成"

  fail_indicators:
    - "缺少任何一行（尤其第 5/6 行）"
    - "第 6 行之前已开始读取项目代码文件"
    - "使用 glob/find_path 扫描 .ai-memory 目录"
    - "记忆路径缺少 projects/<project>/ 前缀（直接写 .ai-memory/...）"

# ═══════════════════════════════════════════════════════════
# F02 — 阶段 0 记忆写入硬性阻塞
# ═══════════════════════════════════════════════════════════

- id: F02
  name: "阶段 0 记忆写入硬性阻塞"
  category: flow-control
  source_ref: "core/QUICK-REFERENCE.md §预检查 L22, core/workflows/common/task-memory.md"

  simulated_input: "分析 permia 项目的权限模型设计"

  expected_behavior:
    - "会话的前三个 tool call 必须严格按序：① now() → ② list_directory(.ai-memory/...) → ③ edit_file(记忆文件)"
    - "三个 tool call 完成前禁止执行任何分析性文件读取"
    - "记忆文件路径：projects/permia/.ai-memory/clients/<agent>/tasks/YYYYMMDD.md，新增 ## 会话 NN（🔄 + 任务概要）"

  checkpoints:
    - "前三个 tool call 顺序严格为 now → list_directory → edit_file"
    - "edit_file 操作的是记忆文件（不是报告文件或其他文件）"
    - "记忆文件路径以 projects/ 开头（非根目录 .ai-memory）"
    - "记忆文件内新增会话段落含 状态/任务概要/对话记录表头"
    - "如果当日记忆文件已存在，追加新会话（不覆盖）"
    - "如果记忆目录不存在，创建目录后再创建文件"

  pass_criteria: "前三个 tool call 严格按序执行，记忆文件在分析前已写入"

  fail_indicators:
    - "前三个 tool call 中插入了读取项目文件的操作"
    - "记忆文件写在错误路径（根目录/.ai-memory 或其他项目目录）"
    - "未创建记忆文件就开始分析"
    - "覆盖了当日已有的记忆文件内容（而不是追加）"

# ═══════════════════════════════════════════════════════════
# F03 — 跨会话任务恢复
# ═══════════════════════════════════════════════════════════

- id: F03
  name: "跨会话任务恢复"
  category: flow-control
  source_ref: "core/QUICK-REFERENCE.md §恢复任务, docs/execution-workflow-guide/02-session-and-modes.md"

  simulated_input: "继续"

  expected_behavior:
    - "关键词'继续'匹配继续任务恢复流程，执行预检查扫描 .ai-memory 最新日期文件"
    - "读记忆（最新会话段落）→ 读 📨 对话记录 → 跟链接读关联报告 → 恢复完整上下文"
    - "向用户汇报：上次任务 + 进度状态 + 待跟进项 + 下一步建议，然后继续执行"

  checkpoints:
    - "只扫描当前 Agent 的记忆目录（Agent 过滤规则）"
    - "读取的是最新日期文件的末尾最新会话（非全部历史）"
    - "恢复汇报中包含上次任务概要和当前进度"
    - "如果上次任务有 IMPLEMENTATION-PLAN，读取 PLAN 找到第一个 🔲 任务继续"
    - "使用 list_directory 逐层扫描（非 glob）"

  pass_criteria: "正确恢复上下文，汇报上次进度，从断点继续"

  fail_indicators:
    - "说'继续'后 AI 要求重新描述需求（未读取记忆）"
    - "读取了其他 Agent 的记忆文件"
    - "使用 glob 扫描 .ai-memory 目录"
    - "恢复时未读取关联报告（只看记忆摘要）"
    - "未向用户汇报恢复的状态就直接开始执行"

# ═══════════════════════════════════════════════════════════
# F04 — 意图推断判定（分析+变更混合场景）
# ═══════════════════════════════════════════════════════════

- id: F04
  name: "意图推断判定（分析+变更混合场景）"
  category: flow-control
  source_ref: "core/workflows/00-task-identification/rules.md §Layer 3 意图推断"

  simulated_input: "分析当前目录结构的合理性，然后给重构方案并执行调整"

  expected_behavior:
    - "🔴 Layer 3 意图推断：Q1=最终目的是重构(变更)，Q2=分析是手段，Q3=有具体事项(重构+调整) → 判定为需求开发（非深度分析）"
    - "按需求开发流程执行 CP1→CP2→CP3，分析只是变更的前置步骤"
    - "预检查第 2 行任务类型标注为'需求开发'"
    - "识别日志中包含意图推断理由（Layer 3 三问结论）"

  checkpoints:
    - "任务类型识别为需求开发（不是深度分析）"
    - "Layer 3 三问已执行并记录推断理由"
    - "输出需求理解 → CP1 暂停（不跳过）"
    - "不在'分析'完成后直接执行变更（必须经过 CP2/CP3 确认）"
    - "预检查中明确说明意图推断判定理由"

  pass_criteria: "Layer 3 意图推断正确识别为需求开发，走完整 CP1→CP2→CP3 流程"

  fail_indicators:
    - "被误判为深度分析（10-analysis）"
    - "分析完成后直接执行变更（跳过确认点）"
    - "在同一回复中输出分析结果和变更执行结果"
    - "未执行 Layer 3 三问或未记录意图推断理由"
    - "仅依赖关键词匹配判定，未分析用户最终目的"

# ═══════════════════════════════════════════════════════════
# F05 — CP1 需求理解后暂停
# ═══════════════════════════════════════════════════════════

- id: F05
  name: "CP1 需求理解后暂停"
  category: flow-control
  source_ref: "core/workflows/common/confirmation-points.md §CP1, 01-requirement-dev §强制执行规则"

  simulated_input: "给 user-service 的 /api/users 接口加上分页参数"

  expected_behavior:
    - "任务识别为需求开发，阶段 1 输出需求理解（核心需求 + 涉及模块 + 关键改动）"
    - "输出确认提示后停止等待用户回复，🔴 即使需求极简也必须暂停"
    - "🔴 禁止在同一回复中输出需求理解 + 技术方案"

  checkpoints:
    - "需求理解包含核心需求一句话复述"
    - "需求理解包含涉及模块和关键改动点"
    - "回复末尾有明确的确认提示和选项"
    - "回复结束后确实停止（不继续输出方案）"
    - "简单需求也不跳过（本用例的需求很简单，正好测试）"

  pass_criteria: "无论需求简单还是复杂，CP1 都暂停并等待确认"

  fail_indicators:
    - "跳过 CP1 直接输出技术方案或分析代码"
    - "在同一回复中同时输出需求理解和技术方案"
    - "以'需求很清晰'为由省略 CP1"
    - "需求理解中没有改动清单"
    - "没有确认提示就结束回复"

# ═══════════════════════════════════════════════════════════
# F06 — CP2 技术方案后暂停
# ═══════════════════════════════════════════════════════════

- id: F06
  name: "CP2 技术方案后暂停"
  category: flow-control
  source_ref: "core/workflows/common/confirmation-points.md §CP2, 01-requirement-dev §阶段2"

  simulated_input: "[用户在 CP1 确认后] 确认"

  expected_behavior:
    - "收到 CP1 确认后，读取相关代码文件（不凭想象），输出技术方案（架构 + 改动清单 + 影响评估）"
    - "输出确认提示后停止等待用户回复，🔴 禁止在方案输出后立即执行代码"

  checkpoints:
    - "技术方案包含改动文件清单（表格格式）"
    - "技术方案包含影响评估和风险等级"
    - "回复末尾有确认提示和选项"
    - "回复中不包含任何代码实现（只有方案描述）"
    - "方案基于实际读取的代码（非推测）"

  pass_criteria: "技术方案完整输出，暂停等待确认，不包含代码执行"

  fail_indicators:
    - "在方案输出的同一回复中包含代码实现"
    - "未读取代码就设计方案（凭想象设计）"
    - "方案中没有改动清单"
    - "方案中没有影响评估"
    - "没有确认提示就结束回复"

# ═══════════════════════════════════════════════════════════
# F07 — CP3 实施方案后暂停
# ═══════════════════════════════════════════════════════════

- id: F07
  name: "CP3 实施方案后暂停"
  category: flow-control
  source_ref: "core/workflows/common/confirmation-points.md §CP3, 01-requirement-dev §阶段3"

  simulated_input: "[用户在 CP2 确认后] 确认"

  expected_behavior:
    - "收到 CP2 确认后，进入阶段 3 实施方案生成"
    - "输出实施方案：实施前检查清单 + 变更文件详情 + 实施步骤 + 验证清单"
    - "同步生成 IMPLEMENTATION-PLAN.md（强制 — 大小需求均需生成）"
    - "输出确认提示：'确认所有变更内容后开始执行？[确认/修改/取消]'"
    - "然后停止，等待用户明确回复"
    - "🔴 用户确认所有变更内容后，才开始执行代码"

  checkpoints:
    - "实施方案包含实施前检查清单"
    - "实施方案包含每个变更文件的详情"
    - "实施步骤按依赖顺序排列"
    - "IMPLEMENTATION-PLAN.md 已生成或在聊天中输出"
    - "小需求：PLAN 含任务编号表 + 实施顺序"
    - "大需求：PLAN 含进度看板 + 任务编号表 + 依赖拓扑 + Phase + 完成标准"
    - "回复中不包含已执行的代码变更"

  pass_criteria: "实施方案 + IMPLEMENTATION-PLAN 完整输出，暂停等待确认，未开始执行"

  fail_indicators:
    - "未生成 IMPLEMENTATION-PLAN.md"
    - "实施方案中没有实施前检查清单"
    - "实施步骤未按依赖顺序排列"
    - "在同一回复中输出实施方案和执行结果"
    - "用户未确认就开始执行代码修改"

# ═══════════════════════════════════════════════════════════
# F08 — 多任务拆分
# ═══════════════════════════════════════════════════════════

- id: F08
  name: "多任务同时提出"
  category: flow-control
  source_ref: "core/workflows/00-task-identification/README.md §情况2, core/CONSTRAINTS.md 约束#4"

  simulated_input: "实现限流功能，并优化查询性能，顺便修复登录报错的 Bug"

  expected_behavior:
    - "检测到 3 个独立任务：需求开发 + 性能优化 + Bug 修复"
    - "向用户列出识别到的所有任务"
    - "建议分开执行（约束 #4：多任务需拆分）"
    - "询问执行顺序（用户决定优先级）"
    - "不直接混合执行多个任务"

  checkpoints:
    - "正确识别出 3 个独立任务"
    - "每个任务标注了任务类型"
    - "建议分开执行（非直接混合执行）"
    - "询问用户确认执行顺序"
    - "等待用户选择后再开始第一个任务"

  pass_criteria: "识别多任务，建议拆分，询问优先级后再执行"

  fail_indicators:
    - "未识别出多个任务（当成一个需求处理）"
    - "直接混合执行多个任务（违反约束 #4）"
    - "未询问执行顺序就自行决定"
    - "一次性输出所有任务的方案"

# ═══════════════════════════════════════════════════════════
# F09 — 小需求统一 7 阶段流程（v2.13.0 FIX-016 重写）
# ═══════════════════════════════════════════════════════════

- id: F09
  name: "小需求统一 7 阶段流程"
  category: flow-control
  source_ref: "core/workflows/01-requirement-dev/README.md §统一7阶段流程, core/QUICK-REFERENCE.md §执行流程"

  simulated_input: "帮我在配置文件里加一个超时参数"

  expected_behavior:
    - "小需求（< 5 个文件、单模块）→ 使用 lite 模板，但仍走统一 7 阶段流程"
    - "CP1→CP2→CP3 确认点完整流程（不因需求简单而跳过）"
    - "IMPLEMENTATION-PLAN 简化版（任务编号表 + 实施顺序）"
    - "阶段 6 归档文档必须生成文件（内容可精简，但 01-需求定义.md / 02-技术方案.md 文件必须存在）"
    - "阶段 7 验证检查"

  checkpoints:
    - "流程为统一 7 阶段（不是 5 阶段）"
    - "使用 lite 模板（精简内容）"
    - "确认点流程完整（CP1→CP2→CP3）"
    - "归档文档文件已生成（小需求内容精简但文件存在）"

  pass_criteria: "小需求走完整 7 阶段流程，使用 lite 模板，归档文档文件存在（内容可精简）"

  fail_indicators:
    - "跳过阶段 6/7（归档文档和验证检查）"
    - "跳过确认点（CP1/CP2/CP3）"
    - "未生成归档文档文件（01-需求定义.md 等）"
    - "未使用 lite 模板（小需求误用 core 模板）"

# ═══════════════════════════════════════════════════════════
# F10 — 大需求统一 7 阶段流程（v2.13.0 FIX-016 重写）
# ═══════════════════════════════════════════════════════════

- id: F10
  name: "大需求统一 7 阶段流程"
  category: flow-control
  source_ref: "core/workflows/01-requirement-dev/README.md §统一7阶段流程, core/QUICK-REFERENCE.md §执行流程"

  simulated_input: "在 user-service 项目实现 OAuth2.0 第三方登录功能，包括微信、Google、GitHub 三个渠道"

  expected_behavior:
    - "大需求（>= 5 个文件、多模块协作）→ 使用 core 模板，走统一 7 阶段流程"
    - "CP1→CP2→CP3 确认点完整流程"
    - "IMPLEMENTATION-PLAN 完整版（进度看板 + 任务编号表 + 依赖拓扑）"
    - "阶段 6 归档文档完整版（01-需求定义.md + 02-技术方案.md + 03-实施方案/）"
    - "阶段 7 验证检查"

  checkpoints:
    - "流程为统一 7 阶段"
    - "使用 core 模板（完整内容）"
    - "生成 01-需求定义.md / 02-技术方案.md / 03-实施方案/ / IMPLEMENTATION-PLAN.md"
    - "02-技术方案超 1000 行时按目录拆分，文件名中文命名（FIX-011）"
    - "文档存放于 projects/<project>/requirements/<中文描述>/"

  pass_criteria: "大需求走完整 7 阶段流程，使用 core 模板，生成完整文档套件，路径和命名规范"

  fail_indicators:
    - "跳过阶段 6/7"
    - "缺少归档文档（01-需求定义/02-技术方案）"
    - "文件名使用英文而非中文（违反 FIX-011）"
    - "文档未存放于 requirements/<中文描述>/ 目录"
    - "未生成 IMPLEMENTATION-PLAN.md"

# ═══════════════════════════════════════════════════════════
# F11 — [DEPRECATED] 模式切换（v2.13.0 废弃）
# ═══════════════════════════════════════════════════════════
# 🔴 v2.13.0 起移除快速/完整双模式（FIX-016），不再存在模式切换场景。
# 本用例保留作为历史参考，不再执行。
# 历史背景：v2.0.0~v2.12.0 支持快速/完整模式切换，因 issue#11/#14/#15
# 多次流程跳过问题，v2.13.0 统一为单一 7 阶段流程。

# ═══════════════════════════════════════════════════════════
# F12 — 阶段 4.5 执行合规性回溯检查（出口门禁）
# ═══════════════════════════════════════════════════════════

- id: F12
  name: "阶段 4.5 执行合规性回溯检查"
  category: flow-control
  source_ref: "core/self-fix/triggers/auto-triggers.md §场景10, core/QUICK-REFERENCE.md §消息驱动"

  simulated_input: "[任务即将结束时] — 无需用户输入，AI 自动执行"

  expected_behavior:
    - "在任务结束前（阶段 4 之前），自动执行阶段 4.5 出口门禁"
    - "检查 3 项必做内容："
    - "  1. 预检查完整性 — 第 6 行是否输出、记忆是否创建"
    - "  2. 确认点遵守 — CP1→CP2→CP3 是否按序执行（纯分析任务可跳过）"
    - "  3. 记忆/报告写入 — 是否写入报告、是否包含链接和对话记录"
    - "🆕 4. 文档产物完整性 — 应有文档是否存在（统一 7 阶段流程，所有需求均须生成归档文档）"
    - "可立即修复的项目（如报告未写入）→ 自动修复"
    - "无法修复的项目（如确认点已跳过）→ 记录到记忆待跟进"
    - "与阶段 0（入口门禁）形成入口-出口闭环"
    - "🆕 在记忆文件中写入 '### 🔍 阶段 4.5 合规性检查' 段落（4 行表格），即使与阶段 4 合并执行也必须留下可审计记录"

  checkpoints:
    - "阶段 4.5 在阶段 4（最终状态更新）之前执行"
    - "3+1 项必做检查均有结论"
    - "可修复项自动修复（不再询问用户）"
    - "不可修复项记录到记忆"
    - "入口门禁（阶段 0）+ 出口门禁（阶段 4.5）形成闭环"
    - "🆕 记忆文件中存在 '### 🔍 阶段 4.5 合规性检查' 段落（含 4 行检查表格），后续会话可回溯验证"

  pass_criteria: "出口门禁自动执行，3+1 项检查完成，可修复项已修复，记忆中有可审计的检查记录"

  fail_indicators:
    - "阶段 4.5 被跳过（直接从阶段 4 到结束）"
    - "检查不完整（遗漏某一项）"
    - "可修复项未自动修复"
    - "出口门禁的检查结果未记录"
    - "🆕 记忆文件中无 4.5 检查段落（合并执行但未留下可审计记录）"

# ═══════════════════════════════════════════════════════════
# F13 — 报告+记忆自动输出
# ═══════════════════════════════════════════════════════════

- id: F13
  name: "报告+记忆自动输出"
  category: flow-control
  source_ref: "core/CONSTRAINTS.md 约束#17, core/QUICK-REFERENCE.md §关键规则"

  simulated_input: "[任务完成时] — 无需用户输入，AI 自动执行"

  expected_behavior:
    - "每次会话结束时，自动完成以下操作（不询问用户 — 约束 #17）："
    - "  1. 写入报告文件 → reports/<子目录>/<agent>/YYYYMMDD/NN-<类型>-<简述>.md"
    - "  2. 更新记忆文件 → .ai-memory/clients/<agent>/tasks/YYYYMMDD.md"
    - "  3. 对话中输出摘要 → 结论要点 + 报告文件路径"
    - "报告 NN 序号仅扫描 reports/<子目录>/<agent>/YYYYMMDD/ 目录计算（约束 #19）"
    - "记忆文件使用 YYYYMMDD.md 格式（无序号）（约束 #19）"
    - "记忆文件中包含 📨 对话记录表（4 列表格）"
    - "记忆文件中包含报告链接引用"
    - "报告头部状态从 📝 进行中 → ✅ 已完成（阶段 4）"

  checkpoints:
    - "报告文件确实被创建（非仅在聊天中输出）"
    - "报告路径格式正确（含 agent + YYYYMMDD 目录层级）"
    - "报告 NN 序号正确（仅扫描对应目录，非全局）"
    - "记忆文件已更新（含对话记录 + 报告链接）"
    - "对话中输出了报告文件路径"
    - "全程未询问用户'是否写入报告/记忆'"

  pass_criteria: "报告+记忆自动写入，路径和序号规范，未询问用户"

  fail_indicators:
    - "询问用户'是否需要写入报告？'或'要我更新记忆吗？'（违反约束 #17）"
    - "对话中堆砌大段分析内容而不写报告文件（违反约束 #17）"
    - "会话结束时没有报告文件产出"
    - "报告和记忆共享全局序号池（违反约束 #19）"
    - "记忆文件中缺少 📨 对话记录表"
    - "记忆文件中缺少报告链接引用"
```

---

## 📎 相关文档

- [README.md](./README.md) — 走查机制总览
- [01-workflow-scenarios.md](./01-workflow-scenarios.md) — 工作流正常路径场景
- [03-edge-case-scenarios.md](./03-edge-case-scenarios.md) — 边界/异常场景

---

**最后更新**: 2026-03-04