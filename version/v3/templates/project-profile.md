# 项目规范 — [项目名]

> 本文件是 AI 理解项目上下文的核心入口。每次涉及代码操作时，AI 应在预检查阶段读取本文件。
> 使用时将 `[占位符]` 替换为实际内容，删除不适用的章节。

**最后更新**: YYYY-MM-DD

---

## 项目概述

| 项 | 内容 |
|----|------|
| **项目名称** | [项目名] |
| **一句话描述** | [项目做什么？解决什么问题？] |
| **项目类型** | [应用 / 库 / CLI 工具 / 框架 / 插件 / 服务] |
| **当前版本** | [vX.Y.Z] |
| **仓库地址** | [URL 或本地路径] |
| **许可证** | [MIT / Apache-2.0 / ISC / 私有 / ...] |

---

## 技术栈

| 类别 | 技术 | 版本要求 |
|------|------|---------|
| **语言** | [TypeScript / JavaScript / Python / ...] | [版本要求] |
| **运行环境** | [Node.js / 浏览器 / Deno / Bun / ...] | [>=XX] |
| **框架** | [Express / Fastify / React / Vue / 无 / ...] | [版本] |
| **数据库** | [PostgreSQL / MySQL / MongoDB / SQLite / 无 / ...] | [版本] |
| **ORM** | [Prisma / TypeORM / Drizzle / Sequelize / 无 / ...] | [版本] |
| **测试框架** | [vitest / jest / mocha / node:test / ...] | [版本] |
| **包管理器** | [npm / pnpm / yarn] | [版本] |
| **模块系统** | [ESM / CJS / 双模式] | — |

---

## 目录结构

```text
[项目名]/
├── src/              # [源代码说明]
│   ├── index.ts      # [入口文件说明]
│   └── ...
├── test/             # [测试文件说明]
├── docs/             # [文档目录说明]
├── scripts/          # [脚本目录说明]
├── package.json
├── tsconfig.json     # [如适用]
└── README.md
```

> 按实际项目结构修改，删除不存在的目录，补充项目特有的目录。

---

## 命名约定

| 类别 | 约定 | 示例 |
|------|------|------|
| **文件名** | [kebab-case / camelCase / PascalCase] | `user-service.ts` |
| **目录名** | [kebab-case / camelCase] | `data-access/` |
| **类名** | [PascalCase] | `UserService` |
| **函数名** | [camelCase] | `getUserById` |
| **常量名** | [UPPER_SNAKE_CASE] | `MAX_RETRY_COUNT` |
| **接口/类型** | [PascalCase + I前缀 / 无前缀] | `IUserOptions` / `UserOptions` |
| **数据库表** | [snake_case / camelCase] | `user_roles` |
| **API 路由** | [kebab-case] | `/api/user-profiles` |

---

## 编码规范

### 导入顺序

```text
1. 外部依赖（node_modules）
2. 内部模块（项目内绝对路径）
3. 类型导入（type imports）
4. 相对路径导入
```

### 错误处理

| 规则 | 说明 |
|------|------|
| async 函数 | [必须 try/catch 或 .catch() / 由上层统一处理 / ...] |
| 错误类型 | [使用自定义错误类 / 原生 Error / ...] |
| 错误日志 | [使用 logger.error() / console.error / ...] |
| 错误响应 | [统一错误响应格式：{ code, message, details } / ...] |

### 类型安全

| 规则 | 说明 |
|------|------|
| `any` 使用 | [禁止 / 允许但需注释理由 / ...] |
| 严格模式 | [strict: true / false] |
| 类型导出 | [从 types.ts 统一导出 / 就近定义 / ...] |

### 其他约定

<!-- 补充项目特有的编码约定，删除不适用的项 -->

| 规则 | 说明 |
|------|------|
| [约定1] | [说明] |
| [约定2] | [说明] |

---

## 依赖管理

### 核心依赖

| 包名 | 用途 | 版本约束 | 说明 |
|------|------|---------|------|
| [包名] | [用途] | [^X.Y.Z / ~X.Y.Z] | [注意事项] |

### 开发依赖

| 包名 | 用途 | 版本约束 |
|------|------|---------|
| [包名] | [用途] | [版本] |

### 依赖规则

| 规则 | 说明 |
|------|------|
| 添加新依赖 | [需要评估 / 自由添加 / 需审批 / ...] |
| 版本锁定 | [使用 lock 文件 / 宽松版本范围 / ...] |
| 安全更新 | [定期检查 / 自动更新 / ...] |

---

## 构建与运行

### 常用命令

| 命令 | 说明 |
|------|------|
| `npm install` | 安装依赖 |
| `npm run build` | [构建项目] |
| `npm run dev` | [开发模式运行] |
| `npm test` | [运行测试] |
| `npm run lint` | [代码检查] |

### 环境变量

| 变量名 | 说明 | 默认值 | 必须？ |
|--------|------|--------|:------:|
| `[VAR_NAME]` | [说明] | [默认值] | ✅/🟡 |

> 🔴 环境变量存放在 `.env` 文件中（已加入 `.gitignore`），禁止硬编码到代码中（约束 #3）。

---

## 测试规范

| 项 | 说明 |
|----|------|
| **测试目录** | [test/ / __tests__/ / src/**/*.test.ts / ...] |
| **命名约定** | [*.test.ts / *.spec.ts] |
| **运行命令** | [npm test / npx vitest / ...] |
| **覆盖率要求** | [≥80% / 无硬性要求 / ...] |
| **测试类型** | [单元测试 / 集成测试 / E2E / ...] |

---

## Git 约定

| 项 | 说明 |
|----|------|
| **分支策略** | [main + feature 分支 / GitFlow / Trunk-based / ...] |
| **提交规范** | [Conventional Commits / 无特殊要求 / ...] |
| **提交前缀** | `feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:` |
| **PR 流程** | [需要 review / 自主合并 / ...] |

---

## 项目特有规则

> 此章节记录不属于上述分类的项目特有规则和约定。AI 在处理该项目时必须遵守。

<!-- 示例：
- API 响应统一使用 `{ success: boolean, data: T, error?: string }` 格式
- 数据库迁移必须使用 Prisma Migrate，禁止手动修改 schema
- 所有公开 API 必须有 Swagger 文档注释
- 前端组件必须使用 Composition API，禁止 Options API
-->

| 规则 | 说明 | 原因 |
|------|------|------|
| [规则1] | [说明] | [为什么有这条规则] |
| [规则2] | [说明] | [为什么有这条规则] |

---

## 已知限制与注意事项

> 记录项目当前的已知限制、技术债务、需要 AI 特别注意的事项。

| 项 | 说明 | 优先级 |
|----|------|:------:|
| [限制1] | [说明] | 🔴/🟡/💡 |
| [限制2] | [说明] | 🔴/🟡/💡 |

---

## 相关文档

| 文档 | 路径 | 说明 |
|------|------|------|
| README | [项目根/README.md] | 项目说明 |
| CHANGELOG | [项目根/CHANGELOG.md] | 变更日志 |
| API 文档 | [docs/api.md 或 URL] | API 参考 |
| 架构文档 | [docs/architecture.md] | 架构说明 |
| 部署文档 | [docs/deployment.md] | 部署指南 |

---

> **模板版本**: v3.0.0 (2026-03-10)
> **使用说明**: 复制本模板到 `projects/<project>/profile/README.md`，
> 将所有 `[占位符]` 替换为实际内容，删除不适用的章节和示例注释。